import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
    },
    sectionHeader: {
      fontSize: 28,
      paddingLeft: 15,
      paddingTop: 20,
      fontWeight: "bold"
    },
  });

export default styles;